- Action 输入/输出统一在 `action.yml` 的 `inputs:` / `outputs:` 块中以键值对声明，并通过 `${{ inputs.* }}` / `${{ steps.generate.outputs.* }}` 引用。
- README 提供简体中文、繁体中文、English 三个镜像文件，各文件顶部导航互相链接以维持内容同步。
- CI 工作流使用 `permissions.contents: read` 最小权限原则，仅在需要提交变更的工作流中才授予 `contents: write`。